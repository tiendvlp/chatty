query getChannels ($count: Int!) {
    getChannels (count: $count) {
        id,
        title,
        admin,
        status {
            senderEmail,
            description {
                type,
                content
            }
        },
        members {
            id,
            email,
            name,
            avatar {
                type,
                content
            }
        },
        seen,
        createdDate,
        latestUpdate
    }
}
