query getMessages ($count: Int!, $channelId: String!) {
    getMessages(count: $count, channelId: $channelId) {
        id,
        type,
        content,
        senderEmail,
        createdDate,
        channelId
    }
}