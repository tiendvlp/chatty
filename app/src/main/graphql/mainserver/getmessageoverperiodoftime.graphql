query getMessageOverPeriodOfTime ( $channelId: String!, $from: BigInt!, $to: BigInt!) {
    getMessageOverPeriodOfTime (channelId: $channelId, from: $from, to: $to) {
        id,
        content,
        type,
        senderEmail,
        channelId,
        createdDate
    }
}